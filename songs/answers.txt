(1)
    SELECT AVG(energy)
           AVG(valence)
           AVG(danceability)
    FROM songs
    ORDER BY DESC
    LIMIT 100;
   通过对平均值的分析，可以看出，该用户的喜好与情感状态。
(2)
         引用机器学习方法，对用户行为进行分析，使得不仅仅用平均值分析，而是对每个方面有相应的权值，
   从而是分析结果更精确。
